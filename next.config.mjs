/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['encrypted-tbn0.gstatic.com','https://scontent.fadd2-1.fna.fbcdn.net'],
    },
};

export default nextConfig;
